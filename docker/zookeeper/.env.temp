# Thay VERSION theo tag có sẵn trên quay.io
ZK_IMAGE=quay.io/debezium/zookeeper:2.6
KAFKA_IMAGE=quay.io/debezium/kafka:2.6

# mạng nội bộ dùng cho advertise
KAFKA_DOMAIN=localhost

# user/pass cho SASL/PLAIN (client và inter-broker)
KAFKA_SASL_USER=appuser
KAFKA_SASL_PASSWORD=apppass
KAFKA_INTER_USER=broker
KAFKA_INTER_PASSWORD=brokerpass